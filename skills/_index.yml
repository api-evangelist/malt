generated: '2026-08-17'
method: generated
source: >-
  openapi/malt-exposed-apis-openapi.yml — every operationId referenced below was verified verbatim
  against the spec — plus conventions/malt-conventions.yml, errors/malt-problem-types.yml,
  data-model/malt-data-model.yml and conformance/malt-conformance.yml
note: >-
  Malt publishes no skills, no AGENTS.md and no agent guidance of any kind; nothing was found to
  save verbatim. These three skills are generated by API Evangelist and cover the whole of Malt's
  13-operation published surface — the freelancer billing read path, the SCIM 2.0 write path, and
  the invoice-document path whose contract is easy to get wrong. Each skill front-loads the
  constraints an agent actually trips over at Malt: a bare (not Bearer) Authorization token, no
  idempotency key on user creation, unpaged list responses, base64-in-JSON "PDF" endpoints, and
  errors with no machine-readable codes.
skills:
- file: malt-freelancer-revenue-reconciliation.md
  name: Reconcile Malt freelancer revenue for a period
  summary: >-
    Pull invoices, Malt service charges and received payments over a date range and reconcile them
    into gross billed, platform charges, tax collected and cash received.
  api: openapi/malt-exposed-apis-openapi.yml
  operations:
  - findInvoices
  - findFeeInvoices
  - findPayments
  - getInvoice
  - getFeeInvoice
  token_type: freelancer account token
  mutating: false
- file: malt-scim-user-lifecycle.md
  name: Provision and offboard Malt users via SCIM 2.0
  summary: >-
    Drive an organization's Malt user lifecycle through the SCIM 2.0 Users endpoint — query, create,
    replace, deactivate, delete — including the read-before-create rule that substitutes for the
    idempotency key Malt does not provide, and the deactivate-instead-of-delete offboarding path.
  api: openapi/malt-exposed-apis-openapi.yml
  operations:
  - findUsers
  - getUserById
  - createUser
  - replaceUser
  - modifyUser
  - deleteUser
  token_type: organization token (obtained via a Malt representative)
  mutating: true
- file: malt-invoice-document-retrieval.md
  name: Retrieve a Malt invoice document as a PDF
  summary: >-
    Fetch one invoice or service charge invoice and correctly decode Malt's JSON-wrapped,
    base64-encoded PDF payload, choosing the right endpoint family for the right financial direction.
  api: openapi/malt-exposed-apis-openapi.yml
  operations:
  - findInvoices
  - getInvoice
  - getInvoicePdf
  - findFeeInvoices
  - getFeeInvoice
  - getFeeInvoicePdf
  token_type: freelancer account token
  mutating: false
coverage:
  operations_total: 13
  operations_covered: 13
  skills: 3
not_covered:
  reason: >-
    Malt's published API has no talent-search, profile, mission, offer or messaging operations, so
    no skill can be authored for the marketplace flows the platform is best known for. That absence
    is the finding, not a gap in this index.
